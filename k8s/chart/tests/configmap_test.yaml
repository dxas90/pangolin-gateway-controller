suite: test configmap
templates:
  - configmap.yaml
tests:
  - it: should create ConfigMap
    asserts:
      - isKind:
          of: ConfigMap
      - equal:
          path: metadata.name
          value: RELEASE-NAME-pangolin-gateway-controller-config
      - equal:
          path: metadata.labels["app.kubernetes.io/part-of"]
          value: pangolin-gateway-controller

  - it: should include correct labels
    asserts:
      - isSubset:
          path: metadata.labels
          content:
            app.kubernetes.io/name: pangolin-gateway-controller
            app.kubernetes.io/instance: RELEASE-NAME

  - it: should have data section
    asserts:
      - isNotNull:
          path: data
